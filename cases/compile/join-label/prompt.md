# Compile Layer: join-label

Implement `join_label`.

Requirements:

- return `"{prefix}-{index}"` as an owned `String`
- keep the function signature unchanged
- do not use `format!`
- do not allocate an intermediate `Vec`
- keep the crate Clippy-clean

