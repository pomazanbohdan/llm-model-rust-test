# Semantic Layer: unique-words

Implement `unique_words`.

Requirements:

- split on Unicode whitespace
- lowercase each token
- discard empty tokens
- preserve the first appearance order
- return owned `String` values
- do not use external crates

