{
  "id": "edition2024.env-bootstrap-guard.0001",
  "category": "edition2024_migration",
  "difficulty": "medium",
  "edition": "2024",
  "tier": "core",
  "source": {
    "type": "synthetic_mutation",
    "name": "edition2024_templates",
    "license_status": "approved",
    "leakage_risk": "low",
    "notes": "Draft edition-specific migration task."
  },
  "task": {
    "instruction": "Update the bootstrap helper for Rust 2024 semantics.",
    "constraints": [
      "Keep the function signature unchanged.",
      "Use an explicit unsafe block.",
      "Add a precise SAFETY comment explaining the single-threaded assumption."
    ],
    "public_api_locked": true,
    "minimal_patch_expected": true
  },
  "workspace": {
    "files": [
      {
        "path": "src/lib.rs",
        "content": "pub fn init_process_env() {\n    std::env::set_var(\"APP_MODE\", \"test\");\n    std::env::remove_var(\"LEGACY_MODE\");\n}\n"
      }
    ]
  },
  "target": {
    "type": "patch",
    "files": [
      {
        "path": "src/lib.rs",
        "content": "pub fn init_process_env() {\n    // SAFETY: This helper is only called during single-threaded startup before any\n    // threads are spawned, so process-wide environment mutation is not concurrent.\n    unsafe {\n        std::env::set_var(\"APP_MODE\", \"test\");\n        std::env::remove_var(\"LEGACY_MODE\");\n    }\n}\n"
      }
    ]
  },
  "validation": {
    "check": false,
    "clippy": false,
    "test": false,
    "fmt": false,
    "doc": false,
    "doctest": false,
    "notes": "Draft edition migration example pending validation."
  },
  "tags": ["edition2024", "unsafe", "std-env"]
}

